-- Copyright 2022 SmartThings
--
-- Licensed under the Apache License, Version 2.0 (the "License");
-- you may not use this file except in compliance with the License.
-- You may obtain a copy of the License at
--
-- http://www.apache.org/licenses/LICENSE-2.0
--
-- Unless required by applicable law or agreed to in writing, software
-- distributed under the License is distributed on an "AS IS" BASIS,
-- WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
-- See the License for the specific language governing permissions and
-- limitations under the License.

-- DO NOT EDIT: this code is automatically generated by ZCL Advanced Platform generator.

local data_types = require "st.matter.data_types"
local log = require "log"
local TLVParser = require "st.matter.TLV.TLVParser"
local DayOfWeekType = require "st.matter.generated.zap_clusters.Thermostat.types.DayOfWeek"
local ModeForSequenceType = require "st.matter.generated.zap_clusters.Thermostat.types.ModeForSequence"
local ThermostatScheduleTransitionType = require "st.matter.generated.zap_clusters.Thermostat.types.ThermostatScheduleTransition"

-----------------------------------------------------------
-- Thermostat command GetWeeklyScheduleResponse
-----------------------------------------------------------

--- @class st.matter.clusters.Thermostat.GetWeeklyScheduleResponse
--- @alias GetWeeklyScheduleResponse
---
--- @field public ID number 0x0000 the ID of this command
--- @field public NAME string "GetWeeklyScheduleResponse" the name of this command
--- @field public number_of_transitions_for_sequence data_types.Uint8
--- @field public day_of_week_for_sequence st.matter.clusters.Thermostat.types.DayOfWeek
--- @field public mode_for_sequence st.matter.clusters.Thermostat.types.ModeForSequence
--- @field public transitions st.matter.clusters.Thermostat.types.ThermostatScheduleTransition
local GetWeeklyScheduleResponse = {}

GetWeeklyScheduleResponse.NAME = "GetWeeklyScheduleResponse"
GetWeeklyScheduleResponse.ID = 0x0000
GetWeeklyScheduleResponse.field_defs = {
  {
    name = "number_of_transitions_for_sequence",
    field_id = 0,
    optional = false,
    is_nullable = false,
    data_type = data_types.Uint8,
  },
  {
    name = "day_of_week_for_sequence",
    field_id = 1,
    optional = false,
    is_nullable = false,
    data_type = DayOfWeekType,
  },
  {
    name = "mode_for_sequence",
    field_id = 2,
    optional = false,
    is_nullable = false,
    data_type = ModeForSequenceType,
  },
  {
    name = "transitions",
    field_id = 3,
    optional = false,
    is_nullable = false,
    data_type = data_types.Array,
    array_type = ThermostatScheduleTransitionType,
  },
}

--- Add field names to each command field
---
--- @param base_type_obj st.matter.data_types.Structure
function GetWeeklyScheduleResponse:augment_type(base_type_obj)
  local elems = {}
  for _, v in ipairs(base_type_obj.elements) do
    for _, field_def in ipairs(self.field_defs) do
      if field_def.field_id == v.field_id and
         field_def.is_nullable and
         (v.value == nil and v.elements == nil) then
        elems[field_def.name] = data_types.validate_or_build_type(v, data_types.Null, field_def.field_name)
      elseif field_def.field_id == v.field_id and not
        (field_def.is_optional and v.value == nil) then
        elems[field_def.name] = data_types.validate_or_build_type(v, field_def.data_type, field_def.field_name)
        if field_def.array_type ~= nil then
          for i, e in ipairs(elems[field_def.name].elements) do
            elems[field_def.name].elements[i] = data_types.validate_or_build_type(e, field_def.array_type)
          end
        end
      end
    end
  end
  base_type_obj.elements = elems
end

--- Builds an GetWeeklyScheduleResponse test command reponse for the driver integration testing framework
---
--- @param device st.matter.Device the device to build this message to
--- @param endpoint_id number|nil
--- @param number_of_transitions_for_sequence data_types.Uint8
--- @param day_of_week_for_sequence st.matter.clusters.Thermostat.types.DayOfWeek
--- @param mode_for_sequence st.matter.clusters.Thermostat.types.ModeForSequence
--- @param transitions st.matter.clusters.Thermostat.types.ThermostatScheduleTransition
--- @return st.matter.st.matter.interaction_model.InteractionResponse of type COMMAND_RESPONSE
function GetWeeklyScheduleResponse:build_test_command_response(device, endpoint_id, number_of_transitions_for_sequence, day_of_week_for_sequence, mode_for_sequence, transitions, interaction_status)
  local function init(self, device, endpoint_id, number_of_transitions_for_sequence, day_of_week_for_sequence, mode_for_sequence, transitions)
    local out = {}
    local args = {number_of_transitions_for_sequence, day_of_week_for_sequence, mode_for_sequence, transitions}
    if #args > #self.field_defs then
      error(self.NAME .. " received too many arguments")
    end
    for i,v in ipairs(self.field_defs) do
      if v.optional and args[i] == nil then
        out[v.name] = nil
      elseif v.is_nullable and args[i] == nil then
        out[v.name] = data_types.validate_or_build_type(args[i], data_types.Null, v.name)
        out[v.name].field_id = v.field_id
      elseif not v.optional and args[i] == nil then
        out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)
        out[v.name].field_id = v.field_id
      else
        out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
        out[v.name].field_id = v.field_id
      end
    end
    setmetatable(out, {
      __index = GetWeeklyScheduleResponse,
      __tostring = GetWeeklyScheduleResponse.pretty_print
    })
    return self._cluster:build_cluster_command(
      device,
      out,
      endpoint_id,
      self._cluster.ID,
      self.ID
    )
  end
  local self_request =  init(self, device, endpoint_id, number_of_transitions_for_sequence, day_of_week_for_sequence, mode_for_sequence, transitions)
  return self._cluster:build_test_command_response(
    device,
    endpoint_id,
    self._cluster.ID,
    self.ID,
    self_request.info_blocks[1].tlv,
    interaction_status
  )
end

--- Initialize the GetWeeklyScheduleResponse command
---
--- @return nil
function GetWeeklyScheduleResponse:init()
  return nil
end

function GetWeeklyScheduleResponse:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

function GetWeeklyScheduleResponse:deserialize(tlv_buf)
  local data = TLVParser.decode_tlv(tlv_buf)
  self:augment_type(data)
  return data
end

setmetatable(GetWeeklyScheduleResponse, {__call = GetWeeklyScheduleResponse.init})

return GetWeeklyScheduleResponse
