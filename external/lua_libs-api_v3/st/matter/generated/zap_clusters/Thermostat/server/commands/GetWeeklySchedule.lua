-- Copyright 2022 SmartThings
--
-- Licensed under the Apache License, Version 2.0 (the "License");
-- you may not use this file except in compliance with the License.
-- You may obtain a copy of the License at
--
-- http://www.apache.org/licenses/LICENSE-2.0
--
-- Unless required by applicable law or agreed to in writing, software
-- distributed under the License is distributed on an "AS IS" BASIS,
-- WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
-- See the License for the specific language governing permissions and
-- limitations under the License.

-- DO NOT EDIT: this code is automatically generated by ZCL Advanced Platform generator.

local data_types = require "st.matter.data_types"
local log = require "log"
local TLVParser = require "st.matter.TLV.TLVParser"
local DayOfWeekType = require "st.matter.generated.zap_clusters.Thermostat.types.DayOfWeek"
local ModeForSequenceType = require "st.matter.generated.zap_clusters.Thermostat.types.ModeForSequence"

-----------------------------------------------------------
-- Thermostat command GetWeeklySchedule
-----------------------------------------------------------

--- @class st.matter.clusters.Thermostat.GetWeeklySchedule
--- @alias GetWeeklySchedule
---
--- @field public ID number 0x0002 the ID of this command
--- @field public NAME string "GetWeeklySchedule" the name of this command
--- @field public days_to_return st.matter.clusters.Thermostat.types.DayOfWeek
--- @field public mode_to_return st.matter.clusters.Thermostat.types.ModeForSequence
local GetWeeklySchedule = {}

GetWeeklySchedule.NAME = "GetWeeklySchedule"
GetWeeklySchedule.ID = 0x0002
GetWeeklySchedule.field_defs = {
  {
    name = "days_to_return",
    field_id = 0,
    optional = false,
    nullable = false,
    data_type = DayOfWeekType,
  },
  {
    name = "mode_to_return",
    field_id = 1,
    optional = false,
    nullable = false,
    data_type = ModeForSequenceType,
  },
}

--- Refer to GetWeeklyScheduleResponse:build_test_command_response for
--- building a test command reponse for the driver integration testing framework

--- Initialize the GetWeeklySchedule command
---
--- @param self GetWeeklySchedule the template class for this command
--- @param device st.matter.Device the device to build this message to
--- @param days_to_return st.matter.clusters.Thermostat.types.DayOfWeek
--- @param mode_to_return st.matter.clusters.Thermostat.types.ModeForSequence

--- @return st.matter.interaction_model.InteractionRequest of type INVOKE
function GetWeeklySchedule:init(device, endpoint_id, days_to_return, mode_to_return)
  local out = {}
  local args = {days_to_return, mode_to_return}
  if #args > #self.field_defs then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.field_defs) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif v.nullable and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(args[i], data_types.Null, v.name)
      out[v.name].field_id = v.field_id
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)
      out[v.name].field_id = v.field_id
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
      out[v.name].field_id = v.field_id
    end
  end
  setmetatable(out, {
    __index = GetWeeklySchedule,
    __tostring = GetWeeklySchedule.pretty_print
  })
  return self._cluster:build_cluster_command(
    device,
    out,
    endpoint_id,
    self._cluster.ID,
    self.ID
  )
end

function GetWeeklySchedule:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

--- Add field names to each command field
---
--- @param base_type_obj st.matter.data_types.Structure
function GetWeeklySchedule:augment_type(base_type_obj)
  local elems = {}
  for _, v in ipairs(base_type_obj.elements) do
    for _, field_def in ipairs(self.field_defs) do
      if field_def.field_id == v.field_id and
         field_def.is_nullable and
         (v.value == nil and v.elements == nil) then
        elems[field_def.name] = data_types.validate_or_build_type(v, data_types.Null, field_def.field_name)
      elseif field_def.field_id == v.field_id and not
        (field_def.is_optional and v.value == nil) then
        elems[field_def.name] = data_types.validate_or_build_type(v, field_def.data_type, field_def.field_name)
      end
    end
  end
  base_type_obj.elements = elems
end

function GetWeeklySchedule:deserialize(tlv_buf)
  return TLVParser.decode_tlv(tlv_buf)
end

setmetatable(GetWeeklySchedule, {__call = GetWeeklySchedule.init})

return GetWeeklySchedule

